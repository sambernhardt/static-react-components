# A lightweight live-reload boilerplate with Sass compiling.
### Install
1. `git clone git@github.com:sambernhardt/sass-livereload-boilerplate.git`
2. `npm install`
3. `npm start`

Bada bing. Bada boom.
